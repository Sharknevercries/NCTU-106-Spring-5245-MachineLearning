using System;
using System.Collections.Generic;
using System.Text;

namespace HW2
{
    public static class Utils
    {
        public static int PseudoCount(int count)
        {
            return count > 0 ? count : 1;
        }
    }
}
